# Daily Log — Thursday, March 12, 2026

## MeritLayer Build Sprint

### Problem: Sign-in loop on meritlayer.ai
- Steve reported clicking sign-in → nothing happened or redirect loop
- Used browser/Peekaboo to diagnose
- Root cause 1: `NEXT_PUBLIC_CLERK_DOMAIN=meritlayer.ai` was set — wrong for custom domain setup, caused auth loop
- Root cause 2: No `/dashboard` route existed — auth redirected back to `/` after sign-in
- Fix: Removed bad env var, created `/dashboard` route, set `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard`

### Problem: tRPC 404 errors on dashboard
- After deploying initial fixes, dashboard loaded but all data calls returned 404
- Console showed `/api/trpc/projects.list` etc — tRPC client calls with no server
- Root cause: Previous Claude Code session deployed code with tRPC client but no tRPC server
- Fix: Deployed clean local build (REST API only) — all routes working

### ember-otter Claude Code Session
- Spawned to build full MeritLayer app (Opus model)
- Built complete feature set (see WORKING.md)
- Also deployed independently but my clean build was already live
- ember-otter additions included in local repo

### Final State at EOD (6 AM local)
- meritlayer.ai LIVE ✅
- Dashboard functional ✅
- DB migrated ✅
- All CRUD APIs working ✅
- Steve hasn't tested full project→upload→AI flow yet

## Infrastructure
All auto-start services verified running at 4 AM and 6 AM cron checks:
- Mission Control UI (port 3000)
- PaperclipAI
- Telegram Adapter
- Paperclip Notifier
