# 2026-03-23 — Daily Log

## Early Morning (4:00 AM EDT)
- Paperclip auto-start crons fired successfully:
  - PaperclipAI: PID 56626
  - Telegram Adapter: PID 56696
  - Paperclip Notifier: PID 56758

## Morning Session (5:15–5:51 AM EDT)

### DrawStack Production Cleanup

Steve noticed the draw detail page showing duplicate invoices (Boston Demolition & Abatement Co. — BD-2026-001.pdf, $42,500 appearing twice) and invoices stuck in "PARSED" status.

While investigating, discovered that **PR #9** ("merge staging AI features to main") had brought a large number of unreleased AI features into production. Steve asked what else was brought back that shouldn't have been.

**Full audit found 7 features live in production that weren't ready:**

1. "Import from PDF" (Smart SOV Generator) — button + modal on SOV tab
2. Draw Assistant Widget — floating widget on every dashboard page
3. AI Budget Anomaly banner — auto-runs on every project page load
4. AI Draw Risk Scorer — runs on every new draw (step 4), no feature flag check
5. Lender AI Insights — on lender draw detail (no flag check)
6. Funding Prediction Card — on project page
7. AI Cost Benchmarking — on project page

**Cleanup PRs:**
- PR #13: Removed Import from PDF button/modal/state/handlers → merged
- PR #14: Removed all other 6 AI features → merged to main ~5:45 AM EDT

All API routes and lib files preserved for future rollout behind feature flags (FLAGS object in lib/feature-flags.ts — all currently `false`).

Build confirmed clean (pnpm build ✓) before each merge.

### Open Issue (unresolved at compaction)
- Duplicate invoice rendering on draw detail — root cause not yet identified
- Invoices in "PARSED" status not progressing to Approved

## Notes
- PR #9 was a large staging→main merge (52 files, +4415 lines) that included features explicitly labeled as unreleased. Need better PR review process before merging AI feature branches.
