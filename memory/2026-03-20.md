# 2026-03-20 - Daily Log

## Morning

- DrawStack dashboard 500 fixed — Prisma schema was out of sync with production DB (11 migrations from blitz not in repo). Fixed via db pull + schema update + redeploy. Live as of ~9:30 AM.
- Stripe prices updated — 6 new prices created at $199/$349/$499 monthly, $1990/$3490/$4990 annual. Old prices archived.
- All Paperclip services running (dev-runner, telegram adapter, notifier, chrome).

## Mid-Morning

- Standalone document upload built and pushed to DrawStack (10:10 AM).

## Afternoon

- CLA-168 complete: G703 columns F+J added to draw wizard. Branch `feature/cla-168-g703-columns` ready for review (12:39 PM).
- CLA-169 complete: Invoice manual SOV mapping UI built. Branch `feature/cla-169-invoice-mapping` ready for review (12:41 PM).
- CLA-170 complete: Workflow status bar + draw pipeline built and pushed (5:13 PM).
- CLA-172 complete: Sub invoice submission built, PR opened (8:30 PM).
- CLA-96 complete: Referral system built, PR opened (8:45 PM).
- Polish fixes complete: lien waiver, /pricing, draw SOV lines, PDF button (9:55 PM).
- Blockers fixed: billing portal, Mike Torres, SOV counter (9:55 PM).

## Product Strategy Discussions (12:30–12:43 PM)

**Sub invite placement:**
- Steve asked where sub invite should live — decided Option B: persistent empty-state CTA on project overview ("No subcontractors added — Invite subs →"), disappears once first sub invited

**Workflow visual:**
- Steve wants a visual workflow orientation for users — queued CLA-170
- Two layers: project-level status bar + draw-level step indicator

**Sub portal full roadmap queued:**
- CLA-171: Phase 1 — invite sub, assign SOV lines, project overview CTA
- CLA-173: Phase 2 — sub read-only dashboard (lines + payment status)
- CLA-172: Phase 3 — sub direct invoice submission (the viral loop — pre-mapped into GC draw review)
- Steve confirmed: build phases in order, long-term goal is sub-direct submission

**Branches pending merge to main:**
- feature/cla-168-g703-columns (CLA-168)
- feature/cla-169-invoice-mapping (CLA-169)
- Steve did not confirm merge method before compaction — need to ask: PRs or direct to main?

**Test invoice used today:**
- Norwood Excavation & Site Services LLC, Invoice NE-2026-0311, $23,975
- Project: 123 Maple Street Needham — Alpine Property Group, Draw 1, 5% retention
- Used to test invoice AI parsing + SOV mapping workflow

---

## 10:00 PM — DrawStack QA + Ship Sprint

Steve directive: "work nonstop until all issues are fixed and DrawStack is ready to be shipped"

### Bugs Fixed (14 commits to main, all deployed):
1. **Mike Torres hardcode** → replaced with "Draw #N" card
2. **Upgrade Plan broken** → wired to Stripe Checkout (`/api/billing/checkout`) using raw fetch (bypasses Node 22 SDK issue) — confirmed `cs_live_` in production
3. **Lien waiver not persisting** → GET /api/projects/[id] wasn't returning `retainagePct` or `lienWaiverRequired` — fixed
4. **SOV counter stale** → added `onSaved` callback to SOVBuilder, calls `loadProject()` on save/edit/delete
5. **Export PDF triggered Submit modal** → PDF button had no onClick, now wired to `handleDownloadPdf`
6. **Approve Draw called PDF handler** → fixed to PATCH status=APPROVED
7. **Request Changes had no handler** → wired to recall modal
8. **Draw buttons not role-aware** → GC sees Submit/Recall, Lender sees Approve/Fund based on status
9. **LenderActions component** was imported but never rendered — now wired into draw detail
10. **Stripe webhook price→plan mapping** → was using wrong env var names (STARTER/PRO/SCALE vs SOLO/BUILDER/PORTFOLIO)
11. **`\n` in env vars** → NEXT_PUBLIC_APP_URL, price IDs — stripped globally via `lib/env.ts`
12. **`proxy.ts` public routes** → added robots/sitemap/og/pricing/terms/privacy to isPublicRoute
13. **middleware.ts conflict** → Next.js 16 uses `proxy.ts`, deleted `middleware.ts`

### SEO/AI Search (all new):
- robots.txt (static file, AI crawlers whitelisted)
- sitemap.xml (Next.js route handler)
- /og dynamic OG image (edge rendered)
- JSON-LD SoftwareApplication structured data on homepage
- Page-level keywords (Rabbet alternative, construction draw management, AIA G702 G703)
- lib/env.ts utility for clean env vars

### Infrastructure:
- Error boundaries (global + dashboard error.tsx)
- 404 not-found page
- Vercel function maxDuration 30s for billing/draws/webhooks
- Lender invite modal updated (directs to project page)
- /pricing redirect (→ /#pricing)

### Verified Live:
- robots.txt → 200 ✅
- sitemap.xml → 200 ✅
- /og → 200 ✅
- Stripe checkout → cs_live_ ✅
- Lien waiver toggle ON on edit page ✅
- Draw detail clean (no hardcode) ✅
- Mobile responsive ✅

### Pending (Steve action needed):
- Submit sitemap.xml to Google Search Console
- Verify drawstack.ai in GSC via GoDaddy DNS TXT record
