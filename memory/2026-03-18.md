# Daily Log — Wednesday, March 18, 2026

## DrawStack — Logo Fix Session (7 AM EDT)

### Problem
Steve reported logo background mismatch on mobile — the DrawStack logo was showing a visible colored box against the app's dark sidebar and landing page backgrounds.

### Root Cause
`logo-dark-bg.png` was RGB (no alpha channel) — always showed a rectangle background box.
`logo.png` (133x40, RGBA) appeared to have transparency but actually had a **baked-in blue background** (~`#6e88c5`) for the icon area — only the exterior corners were transparent.

### Fix Applied

**Step 1:** Switched all pages from `logo-dark-bg.png` (with `mixBlendMode: screen`) to `logo.png`
- Affected: `app/page.tsx`, `app/(auth)/layout.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`, `app/dashboard/_components/MobileNav.tsx`, `app/dashboard/_components/Sidebar.tsx`
- Commit: `ba66940`

**Step 2:** Discovered `logo.png` itself had a baked-in blue background color in the icon pixels.
Used Python/Pillow to remove pixels within color distance threshold of `#6e88c5`.
Saved result as `public/logo-transparent.png` → overwrote `public/logo.png`.
- Original backed up as `public/logo-original-backup.png`
- Commit: `1b5b96f`

**Status:** Pushed to main, Vercel deploying (~60s). Logo should now float cleanly on any dark background.

### Sidebar bg color: `#0d1628`
### Landing page bg: `#0f172a`

---

## DrawStack — Tab Bar Mobile Fix (earlier, ~6:40 AM EDT)

- Steve reported tabs cut off on mobile ("Documents" not visible)
- Tab nav needed `overflow-x-auto` for horizontal scrolling
- Fix committed to dashboard project page

---

## Vercel Deploy Status (carried over from 2026-03-17)
- **STILL BLOCKED** from March 17 — GitHub integration disconnected
- Steve needs to reconnect at github.com/apps/vercel or Vercel Settings → Git
- All code is in git, just not auto-deploying
- Latest commits are pushing to main but Vercel may not be picking up
- Need to verify if Vercel reconnected or if these commits are still blocked

---

## DrawStack — Invoice Line Item Editing (7:46 AM EDT)
- ✅ Invoice line item editing built and complete

## DrawStack — Builder-Tier Trial Access (2:20 PM EDT)
- ✅ Builder-tier trial access wired up

## Active Work
- DrawStack is primary focus
- No active trading positions
- No CRM integrations active
