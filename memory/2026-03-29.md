# Daily Log — 2026-03-29 (Sunday)

## Hermes Agent Setup

**Goal:** Install and configure Hermes Agent as an autonomous supervisor for the Mac mini.

### What happened:
- Installed `hermes-agent` via npm/pip (v0.5.0)
- Attempted interactive setup via TTY — arrow key navigation failed through OpenClaw exec
- Configured Nous Portal as provider manually via config
- Steve did OAuth login: `hermes model` → browser flow → `BF6F-KYXP` device code → approved
- First attempt: Login succeeded but got "No active paid subscription found"
- Steve showed screenshot: already on Nous Portal Max plan ($100 credits) — payment had just processed
- **Status:** Retrying `hermes model` to pick up subscription — session compacted here

### Next steps (resume after compaction):
1. Steve runs `hermes model` in Terminal — should now show available models under Nous Portal Max
2. Select `nous/hermes-3-70b` (or whatever appears)
3. Run `hermes doctor` to confirm clean state
4. Configure SOUL.md for Hermes (supervisor/watcher persona for DrawStack + Paperclip)
5. Set up Telegram gateway so Hermes has its own channel
6. Wire Hermes to watch event-bus.jsonl and Paperclip stack

### Fallback plan:
If Nous Portal still has subscription issues → use Anthropic direct (Claude key already on machine)

---

## No Active Trades
Money Printer deactivated 2026-03-04. No trading activity.

---

## DrawStack Status (as of this morning)
- LIVE at drawstack.ai
- Google Ads running at $50/day (launched 2026-03-27, conversion tracking needs investigation)
- Lender invite redeem bug pending fix (constraint violation when user already has OrgMember)
- Google Ads check-in reminder set for 2026-03-30 9 AM EDT

---

## DrawStack Subs Tab Audit — 5:07 PM EDT

Coding agent completed subs tab audit. Bug report sent to Telegram.

**Commit merged:** `0b42af4` — "fix: sub tab workflow bugs — nested Neon includes + GC auth mismatch"

Three bugs patched:
1. `GET /api/projects/[projectId]/subcontractors` — removed nested `subInvoices.lines` select (2-level deep, breaks Neon HTTP). Invoice lines now fetched separately and merged in JS.
2. `POST /api/sub/invoices` — replaced nested includes with separate sequential queries (Neon HTTP: no nested includes beyond 1 level).
3. `PATCH /api/sub/invoices/[invoiceId]` — `getSubEmail` now short-circuits for GC org members, preventing GC email collision routing into sub branch and blocking invoice approval.

Build passed (next build clean). Pushed to main.
